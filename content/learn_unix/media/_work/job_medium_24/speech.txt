Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Pipeline job control treats. Option A. The pipeline as a related process group (typically). Option B. Each command as unrelated always. Option C. Only the last PID. Option D. Only the first file. Select the best answer.
