Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. PATH lookup uses absolute candidates by joining. Option A. Each PATH dir + command name. Option B. Only $HOME. Option C. Only /tmp. Option D. Git hooks only. Select the best answer.
