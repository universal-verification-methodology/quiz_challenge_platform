Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Secure deletion of a dot secret should consider. Option A. Backups, shell history, and editor swap files. Option B. Only rm once without sync. Option C. Only chmod +x. Option D. Only renaming. Select the best answer.
