Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Target. Deps means. Option A. Build target when older than depth slash missing. Option B. Always delete Deps. Option C. Ignore Deps. Option D. Only link. Select the best answer.
