Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. rsync dash a for project sync preserves. Option A. Permissions/times/symlinks better than naive cp. Option B. Only names. Option C. Only zip methods. Option D. Only owners if root always. Select the best answer.
