Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Directory sticky bit, t on slash TMP means. Option A. Only file owner and DIR owner slash route can delete their files. Option B. Everyone can delete anything. Option C. No one can create files. Option D disables exec. Select the best answer.
