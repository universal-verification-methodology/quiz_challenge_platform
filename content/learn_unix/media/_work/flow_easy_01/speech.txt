Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Before git push, a useful first check is. Option A. Delete .git. Option B. git status (and commit/stash if dirty). Option C. Format the disk. Option D. Disable Make. Select the best answer.
