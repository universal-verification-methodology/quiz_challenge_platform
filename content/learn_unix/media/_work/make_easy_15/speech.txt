Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. What does Make primarily read to decide what to run? Option A. A random shell history file. Option B. A Makefile with targets, deps, and recipes. Option C. Only the PATH variable. Option D. Git commit messages. Select the best answer.
