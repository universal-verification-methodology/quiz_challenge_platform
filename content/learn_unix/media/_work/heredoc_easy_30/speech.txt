Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. here-doc 'EOF' (quoted delimiter) typically. Option A. Expands $HOME in the body. Option B. Leaves $VAR literal (no expansion). Option C. Deletes the file EOF. Option D. Means the same as left shift <. Select the best answer.
