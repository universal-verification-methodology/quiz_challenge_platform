Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. file[123].log can match. Option A. file1.log file2.log file3.log. Option B. Only file[123].log literal always. Option C. All .c files. Option D. Only dirs. Select the best answer.
