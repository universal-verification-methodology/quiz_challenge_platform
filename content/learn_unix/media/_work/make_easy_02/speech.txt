Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Why mark clean as .PHONY? Option A. So Make ignores the target forever. Option B. So Make always runs the recipe even if a file named clean exists. Option C. So tabs become spaces. Option D. So make help is disabled. Select the best answer.
