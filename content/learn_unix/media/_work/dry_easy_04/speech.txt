Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Echo-first means. Option A. Print the destructive command before running the real one. Option B. Always pipe to /dev/null. Option C. Skip reading the preview list. Option D. Use sudo by default. Select the best answer.
