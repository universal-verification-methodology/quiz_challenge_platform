Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. In if cmd; then, the if tests. Option A. cmd's exit status. Option B. cmd's stdout only. Option C. cmd's name. Option D. PATH. Select the best answer.
