Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. A relative symlink's target is resolved from. Option A. Always the user's home directory. Option B. The directory containing the symlink. Option C. Only /tmp. Option D. Git's remote URL. Select the best answer.
