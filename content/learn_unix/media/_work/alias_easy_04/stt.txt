Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Type greet after defining greet, concat, typically shows it as A option A directory. Option B function. Option C broken Simlink. Option D kernel module. Select the best answer.
