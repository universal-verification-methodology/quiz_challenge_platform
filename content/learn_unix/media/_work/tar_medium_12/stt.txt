Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Zip encryption features are. Option A, common in zip tooling. TAR usually relies on outer encryption. Option B, identical to TAR. Option C for Bidden. Option D, kernel only. Select the best answer.
