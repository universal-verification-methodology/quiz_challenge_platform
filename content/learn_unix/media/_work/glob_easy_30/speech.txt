Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. data_?.log matches which idea? Option A. Any number of characters after data_. Option B. Exactly one character between data_ and .log. Option C. Only the literal string data_?.log. Option D. Every file in the tree. Select the best answer.
