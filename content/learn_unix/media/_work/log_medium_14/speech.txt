Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Correlate multi-service failures using. Option A. Shared request IDs / timestamps. Option B. Only hostnames. Option C. Only colors. Option D. Only file size. Select the best answer.
