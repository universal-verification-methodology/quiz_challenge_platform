Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Zip stores. Option A. Per-member compression commonly. Option B. Only one stream always like tar+gzip. Option C. Only permissions forever identical to tar. Option D. Only symlinks as hard links. Select the best answer.
