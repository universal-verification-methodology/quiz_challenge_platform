Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. URL-like paths, file colon double forward slash, versus FS paths. Option A. Are not the same API, tools differ. Option B. Are identical always. Option C, replace man. Option D, set a mask. Select the best answer.
