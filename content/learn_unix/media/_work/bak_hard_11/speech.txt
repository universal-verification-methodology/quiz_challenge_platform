Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Backup consistency for live DBs may need. Option A. Quiesce/snapshots/agent-aware dumps. Option B. Only cp -r while writing. Option C. Only tar without locks. Option D. Only zip -r data/. Select the best answer.
