Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Dotfiles are typically named like. Option A. bashrc / .config/. Option B. only README.md. Option C. only /etc/passwd. Option D. only Makefile. Select the best answer.
