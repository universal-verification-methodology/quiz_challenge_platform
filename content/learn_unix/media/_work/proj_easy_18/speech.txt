Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. diff dash u is useful because. Option A. It deletes both files. Option B. It produces unified diffs suitable for patches / similar to git diff. Option C. It only works on binary files. Option D. It replaces find. Select the best answer.
