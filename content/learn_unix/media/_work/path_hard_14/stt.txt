Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Path lookup uses absolute candidates by joining. Option A. Each path DIR plus command name. Option B. Only $HOME. Option C. Only slash TMP. Option D. Get Hooks Only. Select the best answer.
