Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Prefer .env.example keys matching. Option A. What the app actually reads. Option B. Random old keys. Option C. Only PATH. Option D. Only HOME. Select the best answer.
