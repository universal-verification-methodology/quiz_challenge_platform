Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. export KEY22=val22 sets. Option A. An environment variable in the shell. Option B. A man section. Option C. A umask only. Option D. A signal. Select the best answer.
