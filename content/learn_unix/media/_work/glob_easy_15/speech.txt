Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. In a shell glob, * matches. Option A. Exactly one character. Option B. Any sequence of characters (including none). Option C. Only digits. Option D. Only hidden files. Select the best answer.
