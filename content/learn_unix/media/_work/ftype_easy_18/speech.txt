Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. rm on a symlink typically. Option A. Deletes only the link name, not the target file. Option B. Always deletes the target file too. Option C. Formats the disk. Option D. Converts the link into a directory. Select the best answer.
