Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. chmod 600 ~/.ssh/id_rsa matters because. Option A. SSH refuses overly open private keys. Option B. SSH requires 777. Option C. It sets PATH. Option D. It disables keys. Select the best answer.
