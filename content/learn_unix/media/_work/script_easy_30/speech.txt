Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. if [ dash f "$file" ]; then ... checks whether. Option A. The string is empty. Option B. The path exists and is a regular file. Option C. The user is root. Option D. Git is installed. Select the best answer.
