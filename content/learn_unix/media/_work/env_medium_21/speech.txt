Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Precedence often is. Option A. Process env / flags override file defaults (tool-specific). Option B. File always wins. Option C. Random. Option D. Only Makefile. Select the best answer.
