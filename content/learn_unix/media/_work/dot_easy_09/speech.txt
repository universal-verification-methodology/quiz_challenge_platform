Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Editing ~/.bashrc affects. Option A. Interactive bash startup (typically). Option B. The kernel scheduler. Option C. Only cron on Linux always. Option D. Only zip. Select the best answer.
