Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. false or echo fallback typically. Option A. Skips echo because false failed. Option B. Runs echo because false failed (non-zero). Option C. Deletes the file named fallback. Option D. Always exits the shell. Select the best answer.
