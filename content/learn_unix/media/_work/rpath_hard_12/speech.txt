Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Canonicalization across mount namespaces may differ because. Option A. Different mounts/views per process. Option B. inodes cannot exist. Option C. PATH is ignored. Option D. Symlinks are banned. Select the best answer.
