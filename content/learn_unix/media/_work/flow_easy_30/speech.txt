Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. If make test fails with TOOLS not set, you should first. Option A. Rewrite all RTL. Option B. Fix or source the environment / .env (TOOLS path). Option C. rm -rf src. Option D. Ignore exit codes forever. Select the best answer.
