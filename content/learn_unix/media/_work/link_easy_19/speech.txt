Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. A relative symlink target is resolved from. Option A. The directory containing the symlink. Option B. Your cwd only always. Option C. $HOME only. Option D. /tmp only. Select the best answer.
