Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Multiple symlinks in a chain are. Option A. Followed until a final non-link (with limits). Option B. Ignored after the first. Option C. Converted to hard links. Option D. Stored in PATH. Select the best answer.
