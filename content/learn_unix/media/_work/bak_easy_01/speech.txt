Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. A good order of operations is. Option A. Clean first, then maybe backup. Option B. Backup (or dry-run list), then clean generated dirs. Option C. Delete the whole home directory. Option D. Skip listing and use rm -rf $. Select the best answer.
