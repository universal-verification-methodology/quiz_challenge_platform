Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Redact secrets and logs because Option A logs are widely retained slash shared. Option B logs are never stored. Option C encryption is automatic always. Option D, grep band secrets. Select the best answer.
