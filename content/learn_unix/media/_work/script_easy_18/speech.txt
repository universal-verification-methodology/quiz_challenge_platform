Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. NAME="$ concat 1:-World " means. Option A. Always use World and ignore $1. Option B. Use $1 if set, otherwise World. Option C. Delete argument 1. Option D. Require exactly two arguments. Select the best answer.
