Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. if [ dash f "$file" ]; then checks. Option A. String empty. Option B. Path exists as regular file. Option C. User is root. Option D. Git installed. Select the best answer.
