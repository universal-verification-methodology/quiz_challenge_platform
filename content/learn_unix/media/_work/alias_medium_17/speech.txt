Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Debug unexpected behavior with. Option A. type -a cmd and declare -f. Option B. Only reboot. Option C. Only chmod 777. Option D. Only tar. Select the best answer.
