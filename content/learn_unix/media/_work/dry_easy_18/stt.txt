Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Echo first means. Option A print the destructive command before running the real one. Option B. Always pipe to slash dev slash null. Option C skip reading the preview list. Option D use pseudo by default. Select the best answer.
