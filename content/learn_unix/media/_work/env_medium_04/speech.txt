Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Never source untrusted .env because. Option A. It can execute shell code if not a strict parser. Option B. source only reads integers. Option C. env files cannot harm. Option D. bash ignores source. Select the best answer.
