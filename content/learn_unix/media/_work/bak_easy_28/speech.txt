Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. make distclean (when provided) often removes. Option A. More generated files than clean. Option B. Only README. Option C. Only .git. Option D. Only /usr. Select the best answer.
