Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Capability / SELinux labels can deny access even when mode bits allow because. Option A. MAC policies override DAC allowances. Option B. Modes are ignored always. Option C. Root is banned. Option D. PATH overrides MAC. Select the best answer.
