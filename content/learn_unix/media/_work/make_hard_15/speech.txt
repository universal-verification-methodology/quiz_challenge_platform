Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. $(wildcard ...) evaluated at parse can miss. Option A. Files created later during the build. Option B. All existing files. Option C. Only dirs. Option D. Only PHONY. Select the best answer.
