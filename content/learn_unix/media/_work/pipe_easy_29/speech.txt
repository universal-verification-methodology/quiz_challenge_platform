Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. In cmd1 | cmd2, what happens? Option A. cmd2's stdout becomes cmd1's stdin. Option B. cmd1's stdout becomes cmd2's stdin. Option C. Both commands write only to disk. Option D. The pipe deletes cmd1. Select the best answer.
