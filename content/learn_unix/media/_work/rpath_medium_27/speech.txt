Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Scripts should quote "$(real path "$1")" because. Option A. Paths can contain spaces. Option B. real path bans quoting. Option C. It disables glob. Option D. It sets uid. Select the best answer.
