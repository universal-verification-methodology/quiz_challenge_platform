Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Change LogS-PR checklist items catch. Option A human process gaps automation misses. Option B CPU bugs only. Option C disk firmware only. Option D GPU only. Select the best answer.
