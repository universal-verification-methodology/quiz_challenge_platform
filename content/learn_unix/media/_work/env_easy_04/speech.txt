Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. If make fails with TOOLS unset, a good first step is. Option A. Rewrite all RTL. Option B. Source or export the project env (TOOLS path) then retry. Option C. rm -rf .git. Option D. Disable Make forever. Select the best answer.
