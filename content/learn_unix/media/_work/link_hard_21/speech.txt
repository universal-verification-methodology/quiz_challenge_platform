Look at the browser lab figure on the slide. Question. Given the shell snippet on the slide. Race replacing a symlink atomically uses. Option A. ln -sfn into place / rename swap patterns. Option B. rm then ln with gaps. Option C. chmod only. Option D. touch only. Select the best answer.
