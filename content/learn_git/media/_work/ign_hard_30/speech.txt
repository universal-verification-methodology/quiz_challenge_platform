Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt add dash f build/out30.vcd still works on an ignored path because. Option A. Force add overrides ignore for that invocation. Option B. Ignore is broken. Option C. The file is a remote. Option D. head is detached. Select the best answer.
