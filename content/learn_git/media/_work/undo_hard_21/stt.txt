Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. After accidental reset-dash-hard, recovery often uses. Option A JIT reflog then reset-dash-hard to the old SHA option be JIT Clean FDX only. Option C JIT tag-D option D JIT remote prune. Select the best answer.
