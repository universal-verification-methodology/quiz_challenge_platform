Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT show version 1.0 is useful too. Option A, inspect which commit a tag points to and annotation of present. Option B, change your shell path. Option C, ignore build slash directories. Option D, create a merge conflict automatically. Select the best answer.
