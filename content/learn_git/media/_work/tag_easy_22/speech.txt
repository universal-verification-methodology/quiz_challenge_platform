Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt show v1.0 is useful to. Option A. Inspect which commit a tag points to (and annotation if present). Option B. Change your shell PATH. Option C. Ignore build/ directories. Option D. Create a merge conflict automatically. Select the best answer.
