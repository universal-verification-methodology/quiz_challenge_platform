Look at the browser lab figure on the slide. Question. Given the get snippet on the slide. Get RM-cash-build-slashout21.vcd after ignoring it. Option A stops tracking the file but leaves it on desk. Option B deletes the working tree copy. Option C rewrites all history. Option D pushes a tag. Select the best answer.
