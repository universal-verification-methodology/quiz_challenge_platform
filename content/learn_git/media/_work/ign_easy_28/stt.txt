Look at the browser lab figure on the slide. True or false? Given the git snippet on the slide. If build slash out 28.bcd was already committed, adding it to .git ignore a loan remuse it from history.
