Look at the browser lab figure on the slide. Question. Given the get snippet on the slide. Gitlog grep equals lab 20 searches. Option A, commit messages. Option B, working tree only. Option C, origin URLs. Option D, SSH keys. Select the best answer.
