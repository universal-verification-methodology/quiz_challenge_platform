Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. To bump a sub-module version for teammates, you must. Option A, commit the updated GitLink in the parent repo. Option B. Only edit files locally. Option C, delete.getmetles. Option D, force push main without the GitLink. Select the best answer.
