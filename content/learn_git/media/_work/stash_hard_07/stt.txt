Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT-push-dash-keep-index. Option A stashes unstaged work but leaves the index intact. Option B deletes the index. Option C force pushes. Option D prunes origin. Select the best answer.
