Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT restore notes 5.md, no dash dash staged, on a modified tracked file. Option A unstage but keeps edits. Option B discards unstaged edits to match index slash head. Option C deletes the repo. Option D forces origin. Select the best answer.
