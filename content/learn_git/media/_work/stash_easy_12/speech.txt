Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt stash pop compared with jitt stash apply. Option A. Pop applies the stash and removes it from the list; apply keeps the entry. Option B. Pop only works on remotes; apply only works locally. Option C. They are identical in every case. Option D. Pop never restores file contents. Select the best answer.
