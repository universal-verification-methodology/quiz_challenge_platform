Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT's dash pop compared with JIT's dash apply. Option A, pop applies the stash and removes it from the list. Apply keeps the entry. Option B, pop only work some remotes. Apply only works locally. Option C. They are identical in every case. Option D, pop never restores file contents. Select the best answer.
