Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. To clone and fetch sub-modules in one-step, use. Option A JIT clone-dash recur sub-modules. Option B JIT clone-dashbear only. Option C JIT remote prune. Option D JIT stash pop. Select the best answer.
