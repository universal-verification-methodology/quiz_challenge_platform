Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. After rebase, force push to feature slash lab 12, never main, uses. Option A, JIT push dash dash force with lease. Option B, JIT push without lease always. Option C, JIT tag dash D, Option D, JIT stash. Select the best answer.
