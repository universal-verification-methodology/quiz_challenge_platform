Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT Reset Dash Dash Mix head till the one, default. Option A moves head and resets the index, working tree edits remain. Option B deletes.JIT. Option C forces pushes. Option D tags head. Select the best answer.
