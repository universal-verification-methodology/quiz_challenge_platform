Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. Fix up and rebase-i-option-a folds the commit into the previous and drops its message. Option-B pushes origin. Option-C starts by sect. Option-D, delete remotes. Select the best answer.
