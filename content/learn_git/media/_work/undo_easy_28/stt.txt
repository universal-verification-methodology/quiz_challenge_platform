Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT restore notes.md without dash dash staged on a modified tracked file. Option A unstitch the file but keep your edits. Option B discards unstaged edits and matches the last committed slash index state. Option C deletes the entire repository. Option D forces to origin. Select the best answer.
