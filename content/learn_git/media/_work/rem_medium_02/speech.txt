Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt push origin dash dash force with lease feature/lab2 is safer than dash dash force because. Option A. It refuses if the remote moved since your last fetch. Option B. It never rewrites. Option C. It only tags. Option D. It ignores lease. Select the best answer.
