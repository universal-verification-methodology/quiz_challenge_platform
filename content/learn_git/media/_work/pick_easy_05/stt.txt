Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT cherry pick a 005 B mainly. Option A applies that commits changes as a new commit on the current branch. Option B deletes the original commit everywhere. Option C replaces merge for all branches. Option D. Only works on empty repose. Select the best answer.
