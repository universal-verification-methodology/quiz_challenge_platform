Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT stash push mainly. Option A, deletes all history. Option B saves uncommitted tracked changes and cleans the working tree. Option C, force pushes origin. Option D, creates a remote branch. Select the best answer.
