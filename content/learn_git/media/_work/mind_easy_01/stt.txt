Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. The staging area, index, is where you. Option A, store the remote URL forever. Option B, prepare changes for the next commit. Option C, edit files on disk only. Option D, delete the dot git directory. Select the best answer.
