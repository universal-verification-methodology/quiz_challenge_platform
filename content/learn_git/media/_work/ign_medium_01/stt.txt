Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Get RM-cash-build-slashout1.bcd after ignoring it. Option A stops tracking the file but leaves it on disk. Option B deletes the working tree copy. Option C rewrites all history. Option D pushes a tag. Select the best answer.
