Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. A typical chip lab template includes Option A RTL source, test bench, and scripts directories Option B. Only Wade dumps and build artifacts Option C. No read me or get ignore Option D. A second dot JIT inside every file Select the best answer.
