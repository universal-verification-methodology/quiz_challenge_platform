Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. After you fix a conflicted file, the next steps are usually. Option A, get add the file, then get commit to finish the merge. Option B, delete .git and recloning. Option C, git-clear only. Option D, force push without reading the file. Select the best answer.
