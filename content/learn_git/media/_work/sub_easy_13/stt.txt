Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. To clone and fetch sub-majels in one-step, use. Option A JIT clone-dash recur sub-majels. Option B JIT clone-dash bear only. Option C JIT remote prune. Option D JIT stash pop. Select the best answer.
