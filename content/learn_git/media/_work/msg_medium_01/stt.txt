Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Conventional feet slash fix, prefixes are what pick the best answer. Option A optional style that helps change log tooling. Option B required by JIT itself. Option C remote names. Option D ignore patterns. Select the best answer.
