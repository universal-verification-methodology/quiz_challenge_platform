Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. To undo a public commit on main, prefer. Option A JIT revert. Option B JIT reset dash dash hard and force push. Option C deleting dot JIT. Option D stash drop. Select the best answer.
