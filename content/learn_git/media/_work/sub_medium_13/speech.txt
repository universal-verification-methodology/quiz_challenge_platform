Look at the browser lab figure on the slide. True or false. Given the jitt snippet on the slide. Editing files inside a submodule requires a commit in the submodule repo, then a gitlink commit in the parent.
