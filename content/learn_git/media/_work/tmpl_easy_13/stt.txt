Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Use this template on GitHub is for. Option A, creating your own writeable copy from an org template. Option B, deleting remote branches. Option C, running JIT by sacked. Option D, avoiding JIT clone forever. Select the best answer.
