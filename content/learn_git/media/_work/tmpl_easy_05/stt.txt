Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Right after cloning a course template you should. Option A, inspect the layout and run JIT status on a clean tree. Option B, delete dot JIT immediately. Option C, force push to the org template. Option D, skip read me and make file. Select the best answer.
