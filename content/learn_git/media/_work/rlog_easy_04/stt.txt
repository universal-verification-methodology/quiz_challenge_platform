Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. A commit can disappear from JIT log but still appear in ref log when. Option A. No branch points at it anymore after a hard reset. Option B. You only ran JIT status. Option C. The file was never committed. Option D. You used JIT's dash list. Select the best answer.
