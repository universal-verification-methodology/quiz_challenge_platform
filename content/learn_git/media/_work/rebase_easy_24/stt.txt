Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. Jit merge on main typically. Option A joins another branch with a merge commit or fast forward. Option B erases every commit. Option C. Only lists tags. Option D runs only in a browser. Select the best answer.
