Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT sub-module Dana RTL10. Option A unregisters the local checkout can remove the working copy. Option B deletes the remote project. Option C tags head. Option D starts merge tool. Select the best answer.
