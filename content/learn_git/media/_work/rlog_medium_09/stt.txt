Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. Creating a branch at a dangling A009B. Option A gives the commit a ref so GC will not drop it. Option B pushes automatically. Option C deletes origin. Option D starts merge tool. Select the best answer.
