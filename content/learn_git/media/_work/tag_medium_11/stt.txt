Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT tag-d version 1.2.0 deletes. Option A. The local tag. Option B. The remote automatically always. Option C. The commit object always. Option D origin. Select the best answer.
