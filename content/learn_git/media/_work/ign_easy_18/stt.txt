Look at the browser lab figure on the slide. True or false? Given the git snippet on the slide. If build slash out 18.VCD was already committed, adding it to .git ignore a loan removes it from history.
