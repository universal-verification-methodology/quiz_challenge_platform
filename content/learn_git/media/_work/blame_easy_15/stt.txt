Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Git bisect is used to. Option A binary search history for the first bad commit between good and bad tips. Option B, delete all tags. Option C, ignore build slash. Option D, replace merge always. Select the best answer.
