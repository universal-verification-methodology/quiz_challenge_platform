Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. A merge Q slash required checks exist too. Option A block merge until tests pass. Option B replaced JIT locally. Option C delete remotes. Option D skip review. Select the best answer.
