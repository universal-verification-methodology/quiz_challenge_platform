Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt restore dash dash staged notes.md after jitt add. Option A. Removes the file from the staging area but keeps edits on disk. Option B. Deletes the file from disk forever. Option C. Creates a new remote branch. Option D. Rewrites all commit history. Select the best answer.
