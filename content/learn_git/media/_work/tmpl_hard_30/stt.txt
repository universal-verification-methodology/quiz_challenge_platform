Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. After template clone origin should be. Option A. Your rightable repo, not the class read only template if you cannot push there. Option B. Always the org template. Option C. A tag. Option D empty. Select the best answer.
