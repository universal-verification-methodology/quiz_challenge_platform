Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT-branch feature slash lab 12 creates a branch from the stash commit and. Option A applies slash clears that stash onto it. Option B deletes origin. Option C tags head. Option D starts by sect. Select the best answer.
