Look at the browser lab figure on the slide. Question. Given the jet snippet on the slide. Copy a template as a zip without dot jet means. Option A. You lost history. Jidinette starts a new unrelated repo. Option B remote still work. Option C tags remain. Option D fetch works. Select the best answer.
