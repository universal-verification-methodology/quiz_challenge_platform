Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Git switch-C feature slash lab 24 mainly. Option A deletes history. Option B creates the branch and switches to it. Option C forces push as origin. Option D ignores .gitignore. Select the best answer.
