Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. Why jit clone starts with a short ref log? Option A. New clone has little local head motion yet. Option B remotes copy ref log. Option C tags fill ref log. Option D status rights ref log. Select the best answer.
