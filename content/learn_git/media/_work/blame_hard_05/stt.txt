Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Git bisect with merge commits can need. Option A. A test that still compiles. Skip broken snapshots. Option B force push main. Option C deleting remotes. Option D ignoring all RTL. Select the best answer.
