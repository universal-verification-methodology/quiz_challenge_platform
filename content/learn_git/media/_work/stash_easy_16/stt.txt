Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT-pop versus JIT-SPly. Option A, pop applies and drops the entry. Apply keeps it. Option B, pop is remote's only. Option C. They are identical always. Option D, pop never restores files. Select the best answer.
