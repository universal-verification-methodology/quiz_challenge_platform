Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Get by sectrun slashtest.shautomates. Option A good slash bad using the script exit code. Option B push. Option C tag. Option D stash. Select the best answer.
