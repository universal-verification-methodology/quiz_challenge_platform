Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. A force-push to update feature/lab2 after rebase should. Option A. Be expected on a solo feature PR; avoid on shared main. Option B. Rewrite origin/main. Option C. Delete the PR. Option D. Drop tests. Select the best answer.
