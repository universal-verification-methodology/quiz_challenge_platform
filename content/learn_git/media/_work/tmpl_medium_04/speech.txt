Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. If origin still points at a repo you cannot push to. Option A. Change the URL or add a fork remote and push -u there. Option B. Force-push anyway. Option C. Delete dot jitt. Option D. Ignore remotes. Select the best answer.
