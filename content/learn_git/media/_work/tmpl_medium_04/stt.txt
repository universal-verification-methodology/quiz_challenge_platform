Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. If origin still points at a repo you cannot push to. Option A, change the URL or add a fork remote and push you there. Option B, force push anyway. Option C, delete.jet. Option D, ignore remotes. Select the best answer.
