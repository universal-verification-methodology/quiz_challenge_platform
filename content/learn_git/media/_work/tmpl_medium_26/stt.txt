Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. Jit remote at upstream template URL lets you. Option A, fetch teacher updates without pushing to the template. Option B, force push the org repo. Option C, delete your origin. Option D, skip fetch. Select the best answer.
