Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt cherry-pick <hash> mainly. Option A. Applies that commit's changes as a new commit on the current branch. Option B. Deletes the original commit everywhere. Option C. Replaces jitt merge for all branches. Option D. Only works on empty repositories. Select the best answer.
