Look at the browser lab figure on the slide. Question. Given the jit snippet on the slide. A typical lab poll request compares. Option A, lab 25 into main. Option B, main into lab 25 only. Option C, origin into head always. Option D, tags into stash. Select the best answer.
