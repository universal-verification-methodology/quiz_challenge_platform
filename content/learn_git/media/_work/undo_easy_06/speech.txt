Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt restore dash dash staged notes6.md after jitt add. Option A. Removes the path from the index but keeps disk edits. Option B. Deletes the file forever. Option C. Creates a remote branch. Option D. Rewrites all history. Select the best answer.
