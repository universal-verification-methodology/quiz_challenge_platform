Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Git add dash f build slash out 5.bcd still works on an ignored path because Option A force add overrides ignore for that invocation. Option B ignore is broken. Option C. The file is a remote. Option D head is detached. Select the best answer.
