Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. A typical chip-lab template includes. Option A. RTL source, testbench, and scripts directories. Option B. Only wave dumps and build artifacts. Option C. No README or gitignore. Option D. A second dot jitt inside every file. Select the best answer.
