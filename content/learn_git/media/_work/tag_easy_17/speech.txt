Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt show v1.8.0 is useful to. Option A. See which commit the tag points to (and annotation). Option B. Change PATH. Option C. Ignore build/. Option D. Create a conflict. Select the best answer.
