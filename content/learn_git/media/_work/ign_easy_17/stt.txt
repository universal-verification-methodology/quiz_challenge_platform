Look at the browser lab figure on the slide. True or false? Given the git snippet on the slide. After adding build slash to .gitignore, untracked build slash out 17.VCD should vanish from git status.
