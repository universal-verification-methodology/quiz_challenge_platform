Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Copy a template as a zip without .git means. Option A. You lost history. JIT init starts a new unrelated repo. Option B remote still work. Option C tags remain. Option D fetch works. Select the best answer.
