Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT restore dash dash staged notes 2, 4.MD after JIT add. Option A removes the path from the index but keeps disk edits. Option B deletes the file forever. Option C creates a remote branch. Option D rewrites all history. Select the best answer.
