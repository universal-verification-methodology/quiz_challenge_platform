Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. To clone and fetch sub-majels in one-step, use. Option A JIT clone-dash-recurse sub-majels. Option B JIT clone-dash-bear-only. Option C JIT remote-prone. Option D JIT-dash-pop. Select the best answer.
