Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Git merge-dash-abort. Option A cancels the in-progress merge. Option B deletes origin. Option C creates a tag. Option D starts by sect. Select the best answer.
