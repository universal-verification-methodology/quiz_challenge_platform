Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT sub-module Dana RTL30. Option A unregisters the local checkout. Can remove the working copy. Option B deletes the remote project. Option C tags head. Option D starts merge tool. Select the best answer.
