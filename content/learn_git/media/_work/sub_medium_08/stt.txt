Look at the browser lab figure on the slide. True or false? Given the jet snippet on the slide. Editing files inside a sub-module requires a commit in the sub-module repo, then a get link commit in the parent.
