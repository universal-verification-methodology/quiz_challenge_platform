Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. .getmadels records. Option A sub-majal pass and URLs. Option B ignore glob's only. Option C stash. Option D tags. Select the best answer.
