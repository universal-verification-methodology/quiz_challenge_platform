Look at the browser lab figure on the slide. Question. Given the jet snippet on the slide. JIT merge on main typically. Option A joins another branch with a merge commit or fast forward. Option B races every commit in history. Option C. Only lists tags. Option D runs only in the browser lab. Select the best answer.
