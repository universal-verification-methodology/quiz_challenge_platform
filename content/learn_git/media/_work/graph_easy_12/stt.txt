Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Gitlog-dash1 line is useful because it. Option A deletes untracked files. Option B shows a compact list of recent commits. Option C pushes to a remote automatically. Option D creates a new branch. Select the best answer.
