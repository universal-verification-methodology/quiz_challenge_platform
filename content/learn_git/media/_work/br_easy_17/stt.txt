Look at the browser lab figure on the slide. Question. Given the get snippet on the slide. Before creating a branch for shared coursework, you should. Option A branch from any random tip without fetching. Option B update main from the remote, then branch from that tip. Option C delete.get. Option D. Never use a prefix. Select the best answer.
