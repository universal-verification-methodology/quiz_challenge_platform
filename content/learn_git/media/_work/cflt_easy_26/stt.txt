Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. A merge conflict happens when. Option A. Two branches change the same lines and get cannot auto merge. Option B. You run git tag. Option C. You only use log one line. Option D WSL is missing. Select the best answer.
