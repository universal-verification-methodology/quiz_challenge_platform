Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. A merge conflict happens when. Option A. Two branches changed the same lines and jitt cannot auto-merge. Option B. You run jitt tag. Option C. You only use log --oneline. Option D. WSL is missing. Select the best answer.
