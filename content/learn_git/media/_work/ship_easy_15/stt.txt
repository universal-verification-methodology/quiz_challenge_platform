Look at the browser lab figure on the slide. Question. Given the jet snippet on the slide. Core scripts should usually be run from. Option A. The repository route. Rigme and scripts slash live there. Option B. A random subdirectory only. Option C outside the repo. Option D slash TMP with no clone. Select the best answer.
