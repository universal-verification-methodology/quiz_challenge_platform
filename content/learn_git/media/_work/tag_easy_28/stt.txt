Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. A lightweight tag is what pick the best answer. Option A. An aim pointing at a commit without extra tag object metadata. Option B. A branch that auto updates on every push. Option C. The same as .getignore. Option D. Only stored on GitHub. Never locally. Select the best answer.
