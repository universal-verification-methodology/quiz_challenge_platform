Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. A lightweight tag is what - pick the best answer. Option A. A name pointing at a commit, without extra tag object metadata. Option B. A branch that auto-updates on every push. Option C. The same as .gitignore. Option D. Only stored on GitHub, never locally. Select the best answer.
