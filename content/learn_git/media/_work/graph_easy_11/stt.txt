Look at the browser lab figure on the slide. Question. Given the git snippet on the slide. Each new commit in a simple linear history. Option A replaces every older commit permanently. Option B points at its parent and adds a new snapshot. Option C. Only exists in the browser lab. Option D skips the staging area entirely. Select the best answer.
