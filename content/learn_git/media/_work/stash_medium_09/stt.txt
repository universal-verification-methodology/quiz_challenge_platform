Look at the browser lab figure on the slide. Question. Given the jet snippet on the slide. If pop conflicts, the stash entry is often. Option A kept on the stack until you drop it. Option B deleted automatically always. Option C pushed to origin. Option D converted to a tag. Select the best answer.
