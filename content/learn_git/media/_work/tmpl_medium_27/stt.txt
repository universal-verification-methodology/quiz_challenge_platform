Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. First student commit on a template should. Option A. Be on a lab branch, not rewriting shared history. Option B reset dash-hard origin slash main on the org. Option C delete dot getignore. Option D commit build slash. Select the best answer.
