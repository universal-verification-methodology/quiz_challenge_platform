Look at the browser lab figure on the slide. Question. Given the Jet snippet on the slide. Jet restore dash dash stage notes.md after Jet Add. Option A removes the file from the staging area but keeps edits on disk. Option B deletes the file from disk forever. Option C creates a new remote branch. Option D rewrites all commit history. Select the best answer.
