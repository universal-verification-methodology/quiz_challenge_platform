Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. jitt branch dash d feature/lab21 deletes a branch only if. Option A. It is fully merged (safe delete). Option B. It has never been fetched. Option C. It is origin/main. Option D. It is a tag. Select the best answer.
