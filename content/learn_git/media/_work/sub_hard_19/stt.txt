Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT sub-module for each dash dash recursive JIT status. Option A runs a command in each sub-module. Option B pushes parent. Option C deletes pins. Option D starts PR, select the best answer.
