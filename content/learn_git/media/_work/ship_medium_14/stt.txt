Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. If build slash out 14.VCD is generated, submit instructions should. Option A tell graders how to regenerate, not commit the dump. Option B. Always commit every wave. Option C delete RTL. Option D skip tests. Select the best answer.
