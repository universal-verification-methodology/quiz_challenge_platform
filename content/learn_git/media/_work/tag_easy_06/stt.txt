Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. For a published release, prefer. Option A JIT tag-aversion 1.6.0-m-release. Option B deleting.jet. Option C JIT-stash clear only. Option D. Never tagging. Select the best answer.
