Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. After rebase, force push to feature slash lab 17, never main, uses. Option A JIT push-dash-force with lease. Option B JIT push without lease always. Option C JIT tag-d option-d-jit-stash. Select the best answer.
