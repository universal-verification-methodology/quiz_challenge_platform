Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT branch-u-orgent-slab1 feature-slab1 sets feature-slab1 to track. Option A-orgent-slab1. Option B. A tag. Option C. The stash. Option D head-carrot 2. Select the best answer.
