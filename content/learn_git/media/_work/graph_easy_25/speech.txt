Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. Each new commit in a linear history. Option A. Replaces every older commit permanently. Option B. Points at its parent and adds a new snapshot. Option C. Only exists in the browser lab. Option D. Skips the staging area entirely. Select the best answer.
