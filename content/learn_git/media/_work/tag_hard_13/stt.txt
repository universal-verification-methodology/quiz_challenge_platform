Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Why releases use annotated tags? Option A message, tagger, date, tooling and JIT describe prefer them. Option B JIT cannot store lightweight tags on remotes. Option C. They auto move like branches. Option D. Big Nor.Getignore. Select the best answer.
