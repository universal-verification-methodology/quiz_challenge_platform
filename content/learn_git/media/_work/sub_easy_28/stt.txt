Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. After cloning a repo with sub-majals, empty sub-majal folders usually mean. Option A. You still need JIT sub-majal update-dash-init-dash recursive. Option B. The sub-majal was deleted from GitHub. Option C. You must run JIT by sect. Option D sub-majals never use separate repos. Select the best answer.
