Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT restore notes to 3.md, no dash dash staged, on a modified tracked file. Option A unstitch but keeps edits. Option B discards unstaged edits to match index slash head. Option C deletes the repo. Option D forces pushes origin. Select the best answer.
