Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Stash is implemented as. Option A commits reachable from ref slash stash. Option B. A remote. Option C. Attack only. Option D. An ignore rule. Select the best answer.
