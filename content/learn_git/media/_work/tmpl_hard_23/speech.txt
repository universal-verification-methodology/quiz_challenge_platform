Look at the browser lab figure on the slide. Question. Given the jitt snippet on the slide. If Makefile uses rtl23/ relative paths, running it from a subdir. Option A. Often fails; cd to repo root. Option B. Always works. Option C. Pushes origin. Option D. Starts mergetool. Select the best answer.
