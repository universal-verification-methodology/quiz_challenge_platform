Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. Shipping.env.19 with API keys is what pick the best answer. Option A. A security incident. Rotate and use.env.example. Option B required by JIT. Option C. The same as .getignore. Option D. A tag. Select the best answer.
