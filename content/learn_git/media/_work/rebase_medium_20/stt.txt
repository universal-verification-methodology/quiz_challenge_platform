Look at the browser lab figure on the slide. Question. Given the JIT snippet on the slide. JIT rebased-dash-abort. Option A, cancels an in-progress rebase. Option B, deletes origin. Option C, creates a PR option D-prins tags. Select the best answer.
