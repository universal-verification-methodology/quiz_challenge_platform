Look at the browser lab figure on the slide. Question. Why is pure ripple rarely used at 64 bits? Option A. Carry delay grows ~linearly with N. Option B. It cannot add at all. Option C. It needs no gates. Option D. It only works in Gray code. Select the best answer.
