Look at the browser lab figure on the slide. Question. A=0, B=1 with no same-addr clash is… Option A. Independent access (no collision). Option B. Automatic X on both dout. Option C. Both ports must write. Option D. Memory clears to zero. Select the best answer.
