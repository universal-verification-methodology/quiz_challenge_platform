Look at the browser lab figure on the slide. Question. Why use Gray-coded pointers for CDC in an async FIFO? Option A. Only one bit changes per count—safer to synchronize. Option B. Gray is faster than binary always. Option C. Gray removes the need for memory. Option D. Binary pointers are illegal in Verilog. Select the best answer.
