Look at the browser lab figure on the slide. Question. A cache address is commonly split into… Option A. Tag, index (set), and offset (within line). Option B. Opcode and funct only. Option C. Row and column of DRAM only. Option D. Byte enable alone. Select the best answer.
