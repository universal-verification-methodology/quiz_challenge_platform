Look at the browser lab figure on the slide. Question. One-hot input to binary index is an… Option A. expand binary to one-hot. Option B. compress one-hot (or sparse) requests to a binary code. Option C. add carry bits. Option D. only generate clocks. Select the best answer.
