Look at the browser lab figure on the slide. Question. Sync reset idiom is closest to… Option A. always @(posedge clk) if (!rst_n) q<=0; else q<=d;. Option B. always @(*) q = ~rst_n;. Option C. assign clk = rst_n & d;. Option D. always @(negedge d) q<=1;. Select the best answer.
