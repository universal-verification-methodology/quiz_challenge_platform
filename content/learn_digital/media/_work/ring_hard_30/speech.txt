Look at the browser lab figure on the slide. Question. always shift with feedback: ring uses… Option A. q0<=q[MSB] vs q0<=~q[MSB]. Option B. Both use XOR of all taps identically. Option C. Both require gated clocks. Option D. Both forbid shift registers. Select the best answer.
