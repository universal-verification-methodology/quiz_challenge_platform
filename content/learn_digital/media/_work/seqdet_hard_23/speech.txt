Look at the browser lab figure on the slide. Question. From prefix 10, input 0 typically… Option A. 1 → deeper prefix (101); 0 → recovery per table. Option B. Both inputs force S0 always with no recovery. Option C. Both inputs assert Z immediately. Option D. Inputs are ignored until reset. Select the best answer.
