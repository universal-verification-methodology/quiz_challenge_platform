Look at the browser lab figure on the slide. Question. Three-state ring that advances on x=1 is… Option A. A sequenced FSM walked by the input stream. Option B. A pure combo XOR tree. Option C. An async FIFO Gray pointer alone. Option D. A setup-time formula. Select the best answer.
