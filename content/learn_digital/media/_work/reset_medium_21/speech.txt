Look at the browser lab figure on the slide. Question. Asynchronous reset sensitivity typically includes… Option A. negedge rst_n (active-low). Option B. Only posedge of data. Option C. UART framing edges. Option D. Nothing — async never lists rst. Select the best answer.
