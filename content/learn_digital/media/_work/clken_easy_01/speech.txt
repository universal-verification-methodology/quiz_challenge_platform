Look at the browser lab figure on the slide. Question. Preferred RTL style for conditional updates? Option A. Clock enable (if ce) on free-running clk. Option B. Raw AND gate on clk always. Option C. No clock at all. Option D. Only async reset. Select the best answer.
