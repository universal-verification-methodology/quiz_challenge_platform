Look at the browser lab figure on the slide. Question. Same bits, different type: signed −1 vs unsigned is… Option A. Bits change automatically. Option B. Changes the numeric meaning (e.g. −1 → 255). Option C. Never matters in RTL. Option D. Only affects clocks. Select the best answer.
