Look at the browser lab figure on the slide. Question. Mid-cycle active-low reset: async vs sync? Option A. Async can clear immediately; sync waits for posedge. Option B. Both always ignore reset. Option C. Sync always clears first. Option D. The clock stops forever. Select the best answer.
