Look at the browser lab figure on the slide. Question. How are Gray pointers synchronized? Option A. Two flip-flop synchronizers (2FF). Option B. A single XOR gate. Option C. Direct combinational wire. Option D. No synchronization. Select the best answer.
