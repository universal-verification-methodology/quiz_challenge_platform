Look at the browser lab figure on the slide. Question. CPU sb/sh style ops rely on… Option A. Byte enables (or read-modify-write). Option B. Only full-word writes forever. Option C. Gray pointers. Option D. ALU V flag. Select the best answer.
