Look at the browser lab figure on the slide. Question. Pick the invalid Boolean step. Option A. ~(A·B) → ~A + ~B. Option B. ~(A+B) → ~A · ~B. Option C. ~(A·B) → ~A · ~B. Option D. A + A·B → A. Select the best answer.
