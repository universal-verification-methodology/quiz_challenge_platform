Look at the browser lab figure on the slide. Question. If sync reset deasserts before the sampling edge… Option A. Ignored by sync reset if gone before the edge. Option B. Always captured by sync reset. Option C. Required for async FIFO depth. Option D. Impossible in HDL. Select the best answer.
