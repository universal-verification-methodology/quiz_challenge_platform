Look at the browser lab figure on the slide. Question. Clamping to min/max instead of modulo is… Option A. They are identical behaviors. Option B. Saturation clamps to min/max; wrap uses modular bits. Option C. Wrap only happens in floating point. Option D. Saturation ignores width. Select the best answer.
