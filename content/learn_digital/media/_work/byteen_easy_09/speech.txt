Look at the browser lab figure on the slide. Question. Byte-enable bits on write… Option A. Gate which byte lanes update; others keep old data. Option B. Always clear the full word to zero. Option C. Select the CPU opcode. Option D. Disable the clock. Select the best answer.
