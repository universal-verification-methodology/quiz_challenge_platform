Look at the browser lab figure on the slide. Question. Which matches free-running clk with CE? Option A. always @(posedge clk) if (ce) q <= d;. Option B. assign clk = ce & clk;. Option C. always @(*) q = d;. Option D. always @(negedge ce) clk = 1;. Select the best answer.
