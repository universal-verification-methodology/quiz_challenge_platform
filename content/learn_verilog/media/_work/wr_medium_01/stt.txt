Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash lint check number three. Combo block drives why output ridge queue in a clock always suggests. Option A queue is updated procedurally, often a flop. Option B queue must be a wire. Option C queue is a parameter. Option D queue cannot be imports. Select the best answer.
