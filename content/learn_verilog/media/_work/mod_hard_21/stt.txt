Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare-log module M25, input CLK, input count, output cell, and module forward slash forward slash review context. Y, 6 o'clock, slot 21 variant 1 mixing named and positional ports in one instance. Option A. Is a legal slash rejected by tools. Option B. is preferred style. Option C. Only works for clocks. Option D silently ignores named ports. Select the best answer.
