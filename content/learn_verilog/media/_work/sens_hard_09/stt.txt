Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always underscore FFAT, pos edge CLK, beginner fc, sum equals sum plus 1 foot b1, n starvation, combo block never in list of a mixed always. Option A. Can hide bugs until a listed signal toggles. Option B speeds STA option C fixes CDC. Option D required for lint clean. Select the best answer.
