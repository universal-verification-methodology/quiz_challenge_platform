Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog module M13, input CLK, input sum, output den, and module forward slash forward slash review context. Y, phi to clock, slot 11 variant 1.CLK, clock, connects. Option A, formal port CLK to actual net clock. Option B, parameter CLK to 1. Option C. Two modules named CLK and Clock. Option D. Only a wire declaration. Select the best answer.
