Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at Posegg CLK or Nej edge RST underscore N. If RST underscore N, d equals zero, LST equals CLK. In out-by-directional paths need enable disciplines so only one side drives.
