Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog unique case, N, forward slash asterisk, full case required asterisk forward slash always underscore latch exists in SB to mark intentional latches.
