Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash lint check number seven, combo block drives and always underscore comb, SB versus that asterisk. Option A always underscore comb ads checking slash intent for combo. Option B. They differ in net type only. Option C always underscore comb as a wire. Option D identical token. Select the best answer.
