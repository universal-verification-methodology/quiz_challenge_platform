Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. A structural module primarily. Option A. Instantiates other modules / gates and wires them. Option B. Uses only $finish. Option C. Cannot have ports. Option D. Must use only blocking assigns. Select the best answer.
