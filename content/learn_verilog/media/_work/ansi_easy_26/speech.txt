Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Which is classic non-ANSI? Option A. module m(clk, q); input clk; output q;. Option B. module m(input clk, output q);. Option C. always @* q = clk;. Option D. local parameter m = 1;. Select the best answer.
