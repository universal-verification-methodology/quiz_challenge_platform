Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log range, 2 o'clock, count, always at, posedclk, if n, count equals count plus 1, upslash down with DIR. Option A, Q equals DIR. Q plus 1, Q1, with enable slash resets prioritized. Option B. Two always on Q, option C. Only assign number 5. Option D. Only generate a false. Select the best answer.
