Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA log always underscore FFAT, Posegg CLK, BeginF, CE, some equals some plus 1 foot B1, and computation dollar clog 2, depth plus 1, as local perm. Option A, common for pointer widths with extra state. Option B, illegal math. Option C, creates latch. Option D needs NBA. Select the best answer.
