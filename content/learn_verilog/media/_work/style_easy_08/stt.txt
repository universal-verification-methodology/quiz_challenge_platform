Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log wire, 5 o'clock, some assigned an equal some carrot count, data enables often named. Option A, C, N, valid, context. Option B, CLK only. Option C, RST underscore and only. Option D, Gen Bar. Select the best answer.
