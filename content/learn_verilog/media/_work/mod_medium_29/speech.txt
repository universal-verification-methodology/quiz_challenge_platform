Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Port width mismatch on a 8-bit bus often. Option A. Truncates/extends with tool warnings. Option B. Is always a hard syntax error. Option C. Changes the module name. Option D. Disables simulation clocks. Select the best answer.
