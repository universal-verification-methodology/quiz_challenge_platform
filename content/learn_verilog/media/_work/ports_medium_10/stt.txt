Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log the sign out equals, sum and den, sum karat den, dot a, a, dot b, b, dot c, with required c-driven inside. Option a may be okay if c is output only in used. Option b. Always drive c to x from parent. Option c deletes the option d sets per m c, select the best answer.
