Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Intentional transparent latch in ASIC. Option A. Uses enable/level templates deliberately. Option B. Uses only $finish. Option C. Uses only parameters. Option D. Uses only initial. Select the best answer.
