Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at POSEDGE CLK or NIGEDGE RST underscore N. If RST underscore N, data equals zero, L-StateA equals count. Replication is orthogonal to generate. Both are elaboration helpers.
