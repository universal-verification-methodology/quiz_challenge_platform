Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log always at posedgeclk or nijedgerst underscore n if rst underscore n d equals zero else d equals clk incomplete reset clause and a combo decode can latch.
