Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log-reg, 11 o'clock, count, always at, pos-edclk, if n, count equals count plus 1, forward slash forward slash review context, b, 6 o'clock, slot 30 variant 1 teaching bug, if, c, q equals q plus 1, else q equals q plus 1. Option A enabled is nothing, always increments. Option B holds correctly. Option C clears. Option D try states. Select the best answer.
