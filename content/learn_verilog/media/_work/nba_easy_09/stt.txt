Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barolog always at, Posegclk, Begindin equals count, count equals den, end using blocking for a simple flop Q equals D. Option A. Can work in isolation but as bad style slash races in larger code. Option B required by IEEE. Option C same as wire. Option D bands reset. Select the best answer.
