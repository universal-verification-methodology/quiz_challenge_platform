Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Ports appear in the module. Option A. Header (and optionally body for older style). Option B. Only inside always blocks. Option C. Only in $display calls. Option D. Never - modules have no I/O. Select the best answer.
