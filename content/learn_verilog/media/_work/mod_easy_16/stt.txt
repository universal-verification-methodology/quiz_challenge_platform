Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog module M21, input CLK, input CLK, output N, and module forward slash forward slash review context, RST underscore N, 31 colon zero, slot 16 variant 1 ports appear in the module. Option A header, and optionally body for older style. Option B. Only inside always blocks. Option C. Only in dollar display calls. Option D. Never, modules have no I.O. Select the best answer.
