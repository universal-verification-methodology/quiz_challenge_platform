Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instance in Q27, ports wired with dot out, out, forward slash forward slash review context, Q7 o'clock, slot 22 variant 1 and input port on a module. Option A. Is driven from outside into the module? Option B must always drive the parent. Option C. is the same as a parameter. Option D cannot appear on a gate like in two. Select the best answer.
