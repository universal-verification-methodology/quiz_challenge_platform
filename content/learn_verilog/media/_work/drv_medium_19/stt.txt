Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog always at Pose edge CLK or Nej edge RST underscore N. If RST underscore N, CLK equals zero, LCLK equals N, forward slash forward slash review context, A, for a clock, slot 19 variant 1, always underscore comb unique driver rule, SV. Option A helps catch multi-driven variables. Option B creates the option C bands mux. Option D is a port. Select the best answer.
