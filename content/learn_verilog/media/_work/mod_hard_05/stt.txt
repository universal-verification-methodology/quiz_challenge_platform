Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barilog module M9, Input CLK, Input Data, Output Count, and module.y, versus omitting.y entirely. Option A. Both leave Y-on connected, explicit empty is clearer intent. Option B.y, drives Y to zero. Option C omitting always errors. Option D. They select different parameters. Select the best answer.
