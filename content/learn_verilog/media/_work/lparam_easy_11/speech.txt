Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. parameter WIDTH = 8 at the module boundary. Option A. Can be overridden per instance with #(.WIDTH(n)). Option B. Can never change. Option C. Is a wire. Option D. Only TB. Select the best answer.
