Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog parameter W equals 32, Local per max equals 1W, dash 1, forward slash forward slash review context, Q, 7 o'clock, slot 22 variant 1 always at A, Y equals A and B, when only B changes. Option A. The block often does not run, Y can stay stale. option B, why always updates correctly. Option C. A flop is inferred. Option D, Sim must stop. Select the best answer.
