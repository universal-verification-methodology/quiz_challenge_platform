Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. always @(A) Y = A & B; when only B changes. Option A. The block often does not run - Y can stay stale. Option B. Y always updates correctly. Option C. A flop is inferred. Option D. Sim must stop. Select the best answer.
