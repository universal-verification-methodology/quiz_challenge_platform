Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at Posecclk, if load, count equals 10, else if, c, count equals count plus 1, gray counting changes only one bit between adjacent codes.
