Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always underscore FF-AT, pos-edge CLK, begin if, CE, sum equals sum plus 1 foot B1, and sip O' parallel read during shift. Option A. See intermediate values each cycle. Option B frozen until stop always. Option C meet second clock. Option D meets force. Select the best answer.
