Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log parameter W11 equals 4, forward slash forward slash width knob HFF without a size is an unsized text literal.
