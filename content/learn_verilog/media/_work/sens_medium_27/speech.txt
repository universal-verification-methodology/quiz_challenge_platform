Look at the browser lab figure on the slide. True or false. Given the RTL snippet on the slide. Both positive edge and negative edge of the same clock in one always is unusual/wrong for a simple flop.
