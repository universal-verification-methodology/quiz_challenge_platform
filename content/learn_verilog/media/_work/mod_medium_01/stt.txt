Look at the browser lab figure on the slide. Question. Given this RTL snippet, barelog forward slash forward slash instantu3, ports wired with .y, y, .clk, clock, connects. Option A, formal port CLK to actual net clock. Option B, parameter CLK to 1. Option C. Two modules named CLK and clock. Option D. Only a wire declaration. Select the best answer.
