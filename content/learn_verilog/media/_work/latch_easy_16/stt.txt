Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire, for a clock, CLK, assign an equal CLK carrot RST underscore N, forward slash forward slash review context, RST underscore N, 31 colon zero, slot 16 variant 1 intentional latch enable style is. Option A, when enable is low, hold, when high, pass input. Option B, always pose edge. Option C. Only assign number 5. Option D parameter override. Select the best answer.
