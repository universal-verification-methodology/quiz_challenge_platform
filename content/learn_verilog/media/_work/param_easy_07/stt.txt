Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at POSEDGE CLK, out equal sum, parameters are evaluated at a elaboration, not as runtime changing regs.
