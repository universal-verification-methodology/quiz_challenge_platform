Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog module M9, Input CLK, Input data, Output Count, and module a structural module primarily. Option A instantiates other modules slash gates and wires them. Option B uses only dollar finish. Option C cannot have ports. Option D must use only blocking assigns. Select the best answer.
