Look at the browser lab figure on the slide. True or false? Given this RTL snippet, their log always at posedgeclk or knee-judge RST underscore n. If RST underscore n, d equals zero, else d equals CLK, q equals q1, insert zero at LSB for logical left shift.
