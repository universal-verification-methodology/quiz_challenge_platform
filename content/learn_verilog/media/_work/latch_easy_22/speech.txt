Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. assign Y = S ? D1 : D0; is latch-free because. Option A. Every input combination has a defined Y. Option B. assign only works on reg. Option C. S must be a clock. Option D. D0/D1 constants. Select the best answer.
