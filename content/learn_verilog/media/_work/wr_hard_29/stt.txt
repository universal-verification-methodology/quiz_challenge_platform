Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA log always underscore FF-AT, PoseGCLK, BeginF, C, Y equals Y plus 1 foot B1, and forward slash forward slash review context. A, 5 o'clock, slot 29 variant 1 assigning to an input port from inside the module. Option A. Is a legal slash bad, inputs are driven externally. Option B. is Reset Style. Option C creates a flop. Option D sets width. Select the best answer.
