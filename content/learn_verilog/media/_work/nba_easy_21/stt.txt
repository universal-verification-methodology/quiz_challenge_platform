Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barolog always at, Posegclk, Begin cell equals out, out equals cell, and forward slash forward slash review context. Y, 6 o'clock, slot 21 Bariot 1 and a clocked always block, non-blocking, less than or equal to sign means. Option A. All RHS values are sampled first, then LHS updates happen together. Option B statements never run on a clock edge. Option C. Only wire outputs. Option D. The block is combo. Select the best answer.
