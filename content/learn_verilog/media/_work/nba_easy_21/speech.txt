Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. In a clocked always block, non-blocking (<=) means. Option A. All RHS values are sampled first, then LHS updates happen together. Option B. Statements never run on a clock edge. Option C. Only wire outputs. Option D. The block is combo. Select the best answer.
