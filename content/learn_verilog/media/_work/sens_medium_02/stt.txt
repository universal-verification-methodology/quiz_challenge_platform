Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog is signed Q equals D and CLK, D Carrot CLK, Always at Pose edge CLK or NeJedRST underscore N is a classic. Option A a sync reset flop template. Option B pure combo block. Option C Generative. Option D DPI call. Select the best answer.
