Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Conditional module instantiation via generate if. Option A. Chooses which submodule exists. Option B. Runs both modules every cycle. Option C. Needs NBA. Option D. Needs $finish. Select the best answer.
