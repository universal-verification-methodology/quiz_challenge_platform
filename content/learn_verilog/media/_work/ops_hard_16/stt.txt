Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog Property P20, At, Pose edge CLK, D-HASH tag hashtag 1CLK, and Property Forward slash Forward slash review context, RST underscore N, 31 colon zero, slot 16 variant 1 context determined width for Adder operands. Option A. Can extend operands to the size of the assignment target. Option B ignores target size always. Option C forces one bit. Option D uses only dollar bits in 1995. Select the best answer.
