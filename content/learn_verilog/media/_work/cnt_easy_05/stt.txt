Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare-log-rage, 2 o'clock, count, always at, pos-edge CLK, if n, count equals count plus 1, and 3 bit binary up counter has modulus. Option A8. Option B10. Option C100. Option D3. Select the best answer.
