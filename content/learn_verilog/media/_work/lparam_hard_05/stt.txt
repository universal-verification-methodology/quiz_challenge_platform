Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA log always underscore FFAT, Posegg CLK, BeginF, C, Data equals data plus 1 foot B1, and using parameter for a value that must stay consistent with another overridden perm. Option A prefer local perm derived from the root knob. Option B, expose both always. Option C, Use 2 clocks. Option D, Use force. Select the best answer.
