Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA log always underscore FF-AT, POSEDGE-CLK, BEGINIF, C, data equals data plus 1 foot B1, and intentional transparent latch and ASIC. Option A uses enable slash-level templates deliberately. Option B uses only dollar finish. Option C uses only parameters. Option D uses only initial. Select the best answer.
