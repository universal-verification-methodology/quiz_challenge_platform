Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. 8'sd-1 as an 8-bit signed literal represents. Option A. All ones (−1 in two's complement). Option B. 255 unsigned only. Option C. Illegal always. Option D. A float. Select the best answer.
