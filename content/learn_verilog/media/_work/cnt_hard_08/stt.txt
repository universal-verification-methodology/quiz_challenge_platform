Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at, pos edge CLK, if, load, count equals 10, else if, CE, count equals count plus 1, hierarchical counters, bytes. Option A ripple enables between stages. Option B must be one flat, always only ever. Option C band CE. Option D require try. Select the best answer.
