Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log always at, Pausegclk, beginsl equals out, out equals cell, and a swap with blocking a equals b. B equals a, loses a value without a temp.
