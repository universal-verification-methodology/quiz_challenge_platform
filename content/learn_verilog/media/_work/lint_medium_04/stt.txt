Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log generative, W greater than 4, begin, G6 and comparing X intentionally in RTL. Option A, usually a TB concern, RTL should be X-clean. Option B required. Option C, creates local Chrome. Option D, fixes WNS. Select the best answer.
