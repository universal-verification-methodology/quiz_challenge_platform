Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at, pos edge CLK, beginning equals B, B equals A, and forward slash forward slash review context, RST underscore N, 2 o'clock, slot 26 variant 1 NBA and combo always is. Option A usually discouraged, can surprise. Option B required. Option C same as a sign. Option D. A port style. Select the best answer.
