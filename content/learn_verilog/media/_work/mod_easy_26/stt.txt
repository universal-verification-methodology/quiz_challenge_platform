Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instant U31, ports wired with dot A, A forward slash forward slash review context, RST underscore N, two o'clock, slot 26 variant one ports appear in the module. Option A header, and optionally body for older style. Option B. Only inside always blocks. Option C. Only in dollar display calls. Option D. Never. Modules have no I.O. Select the best answer.
