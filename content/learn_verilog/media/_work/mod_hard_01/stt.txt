Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog module M5, Input CLK, Input D, Output CLK, End module mixing named and positional ports in one instance. Option A. Is a legal slash rejected by tools. Option B. Is preferred style. Option C. Only works for clocks. Option D silently ignores named ports. Select the best answer.
