Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log reg, phi-to-clock, count, always at, pos-edclk, if n, count equals count plus 1, formal, prove wrap at 9 for decade. Option A property on Q sequence slash reset behavior. Option B. Only dollar display. Option C. Only fonts. Option D. Only positional ports. Select the best answer.
