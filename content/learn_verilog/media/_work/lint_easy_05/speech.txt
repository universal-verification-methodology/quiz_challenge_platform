Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. initial in synthesizable RTL. Option A. Usually non-synth / TB oriented (except some RAM hints). Option B. Required for every flop. Option C. Creates ports. Option D. Fixes C D C. Select the best answer.
