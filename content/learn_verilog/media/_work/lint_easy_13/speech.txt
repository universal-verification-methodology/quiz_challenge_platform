Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. always at star if (en) y = d; no else often triggers. Option A. latch-risk. Option B. parse error. Option C. no-delay rule. Option D. systask warning only. Select the best answer.
