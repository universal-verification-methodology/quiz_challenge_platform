Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylogwire, 15 colon zero, RST underscore N, assigned data equals RST underscore N, a module port list order change can silently break positional instantiations.
