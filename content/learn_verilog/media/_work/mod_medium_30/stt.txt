Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire, six o'clock, B, a sine Y equals B, forward slash forward slash review context, B, six o'clock, slot 30 variant one top of design modules are often identified by. Option A. Being the root instance that is not instantiated elsewhere. Option B. Using only wires. Option C. Having no always blocks. Option D file name alone always. Select the best answer.
