Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Load priority over shift. Option A. if (load) ... else if (ce) shift. Option B. Two drivers. Option C. Only else shift first always. Option D. force load. Select the best answer.
