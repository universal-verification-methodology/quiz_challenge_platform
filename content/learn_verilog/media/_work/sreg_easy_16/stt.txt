Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire for a clock, CLK, assign an equal CLK Carrot RST underscore N, forward slash forward slash review context, RST underscore N, 31 colon zero, slot 16 variant 1 direction toward LSB commonly. Option A, Q equals SI, Q, 7 colon 1, example. Option B, Q equals Q Option C, Q equals Q always. Option D, only assign. Select the best answer.
