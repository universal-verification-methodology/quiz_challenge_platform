Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog wire, for a clock, out, assign some equals out, nested module declarations inside another module are classic Berylog style.
