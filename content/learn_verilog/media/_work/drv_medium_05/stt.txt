Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash lint check number 7, combo block drives and aliasing two names to one net with two drivers. Option A. Still contention. Option B fixes contention. Option C creates generate. Option D creates Gen bar. Select the best answer.
