Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire for a clock, CLK, assign an equal CLK carrot RST underscore N, forward slash forward slash review context, RST underscore N, 31 colon zero, slot 16 variant $1 display in RTL module. Option A. Not synthesizable side effect, TB slash debug. Option B creates gates of displays. Option C required ANSI. Option D mained ports. Select the best answer.
