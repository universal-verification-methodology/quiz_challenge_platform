Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Inside always F F @( positive edge clk), flop outputs should use. Option A. non-blocking <=. Option B. blocking = only. Option C. hash-delay before each assign. Option D. initial defaults. Select the best answer.
