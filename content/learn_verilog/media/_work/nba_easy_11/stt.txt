Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog Reggie, always at asterisk A equals B and Y forward slash forward slash review context Y Fiva clock slot 11 variant 1 in a clocked always block non blocking less than or equal to sign means Option A. All RHS values are sampled first then LHS updates happen together. Option B statements never run on a clock edge. Option C. Only wire outputs. Option D. The block is combo. Select the best answer.
