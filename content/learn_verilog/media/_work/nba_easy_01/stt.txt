Look at the browser lab figure on the slide. Question. Given this RTL snippet, Beryllog always at, Posegclk, Beginclk equals n, n equals clk, and in a clock, always block, non-blocking, less than or equal to sine means. Option A. All RHS values are sampled first, then LHS updates happen together. Option B statements never run on a clock edge. Option C. Only wire outputs. Option D. The block is combo. Select the best answer.
