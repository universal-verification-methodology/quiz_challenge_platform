Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. What is the preferred hold pattern for a counter? Option A. One always with F, C, count equals count plus one. Option B. Two always drivers. Option C blocking combo only. Option D, tight as E when idle. Select the best answer.
