Look at the browser lab figure on the slide. True or false? Given this RTL snippet, their log always at POSEDGE CLK or NeJEDGE RST underscore N. If RST underscore N, D equals 0, LST equals CLK, async reset must appear in sensitivity for the classic template.
