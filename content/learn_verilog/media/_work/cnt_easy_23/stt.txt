Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log-reg, 2 o'clock, count, always at, pos-edge CLK, if n, count equals count plus 1, forward slash forward slash review context, d, 11 o'clock, slot 23 variant 1 modulo 10 counter wraps when Q reaches. Option A9, then next becomes 0. Option B 10 stored in 4 bits. Option C 15 Always. Option D1 because C Resets. Select the best answer.
