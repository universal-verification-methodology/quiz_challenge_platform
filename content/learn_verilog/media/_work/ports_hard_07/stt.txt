Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log unique case, cell, forward slash asterisk full case required asterisk forward slash changing a port name breaks named connections that used the old funnel.
