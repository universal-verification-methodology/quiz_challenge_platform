Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barologa signed some equals, Din and count, Din Carrot count, forward slash forward slash review context, RST underscore N, to a clock, slot 26 variant 1 using a clock edge in a block meant to be combo. Option A. You built sequential logic instead. Option B removes latches always. Option C. is at asterisk, option D, is A and S I. Select the best answer.
