Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog wire, 15 colon zero, RST underscore N, assigned data equals RST underscore N, named port connection.y, y means. Option A, connect formal port y to actual net y option B, delete port y from the module. Option C, declare a new module named y option D, set a parameter named y only. Select the best answer.
