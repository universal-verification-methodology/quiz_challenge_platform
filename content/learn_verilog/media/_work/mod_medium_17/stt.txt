Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instant U19, ports wired with .q, q, forward slash forward slash review context, data, two o'clock, slots 17 variant one a structural module primarily. Option A instantiates other modules slash gates and wires them. Option B uses only dollar finish. Option C cannot have ports. Option D must use only blocking assigns. Select the best answer.
