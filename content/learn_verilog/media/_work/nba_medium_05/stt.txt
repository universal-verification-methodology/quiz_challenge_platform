Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at, pos edge CLK, begin n equals RST under score n, RST under score n equals n, and assign versus nBA. Option A is sign as continuous on nets, nBA is procedural. Option B identical. Option C is sign as only flops. Option D NBA drives only input ports. Select the best answer.
