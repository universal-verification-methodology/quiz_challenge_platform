Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog always underscore FFAT, Posegg CLK, BeginF, C, Data equals data plus 1 foot B1, and Instance array with named ports. Option A. Each instance gets the same named mapping pattern. Option B bands named. Option C requires Depprum. Option D requires weight. Select the best answer.
