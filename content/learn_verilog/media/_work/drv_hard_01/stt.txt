Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA log always underscore FFAT, Posegg CLK, BeginF, C, D equals D plus 1 foot B1, and resolving X after contention when one driver goes Z. Option A may recover to driven value after Z, still avoid fights. Option B stays X forever always. Option C becomes parameter. Option D becomes Gen Bar. Select the best answer.
