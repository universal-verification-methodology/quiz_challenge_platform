Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at posedclk, if load, count equals 10, else if, c, count equals count plus 1, forward slash forward slash review context, rst underscore n, 2 o'clock, slot 26 variant 1 up slash down with dir. Option A, Q equals dir. Q plus 1, Q1, with enable slash resets prioritized. Option B. 2 always on Q option C. Only assign number 5. Option D. Only generate a false. Select the best answer.
