Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Up/down with dir. Option A. q <= dir ? q+1 : q-1; with enables/resets prioritized. Option B. Two always on q. Option C. Only assign #5. Option D. Only generate if false. Select the best answer.
