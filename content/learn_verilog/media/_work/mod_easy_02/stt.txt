Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instant U7, ports wired with .n, n and input port on a module. Option A. Is driven from outside into the module? Option B must always drive the parent. Option C. Is the same as a parameter? Option D cannot appear on a gate like N2. Select the best answer.
