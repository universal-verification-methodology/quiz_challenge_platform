Look at the browser lab figure on the slide. Question. Given this RTL snippet, Verilog wire, 31 colon zero, data, assigned count equals data carat cell, assign y equals a and b with y declared ridges. Option a illegal in classic Verilog. Option b required for synthesis. Option c. Only illegal in TB. Option d same as always at asterisk. Select the best answer.
