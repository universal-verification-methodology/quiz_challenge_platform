Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. and on multi-bit a,b uses. Option A. Truth of each operand as a whole (1-bit result). Option B. Per-bit AND into a vector. Option C. Concatenation. Option D. Only signed add. Select the best answer.
