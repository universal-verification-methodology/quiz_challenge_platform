Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog module M29, input CLK, input DIN, output COUNT, and module forward slash forward slash review context, N31 colon zero, slot 25 variant 1.y, versus omitting dot y entirely. Option A. Both leave y unconnected, explicit empty is clearer intent. Option B.y, Drive's Y to 0. Option C, omitting always errors. Option D. They select different parameters. Select the best answer.
