Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barologa signout equals, sum and den, sum caredin, default, y equals zero, in case. Option A closes uncovered items for y option B creates dual clock. Option C is non-ANSI only. Option D removes ports. Select the best answer.
