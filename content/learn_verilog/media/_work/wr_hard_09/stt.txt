Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare a log always underscore FF-AT, pos-edge-CLK, beginner-Ce, sum equals sum plus 1 foot-B1, and assigning to an input port from inside the module. Option A. Is a legal slash-bad, inputs are driven externally. Option B. Is reset style. Option C creates a flop. Option D sets width. Select the best answer.
