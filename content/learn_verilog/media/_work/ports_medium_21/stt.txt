Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash lint check number 23, combo block drives RST underscore and forward slash forward slash review context. Y, 6 o'clock, slot 21 variant 1 dot RST underscore in, intentionally open on an used reset. Option A documents in used port, ensure design allows it. Option B, ties reset low always. Option C deletes flop. Option D sets width. Select the best answer.
