Look at the browser lab figure on the slide. Question. Given this RTL snippet, B. A log wire, 15 colon zero, B. A sine y equals B to the power of Q, forward slash forward slash review context, Q, six o'clock, slot 12 variant one local perm depth equals width asterisk two is meant for. Option A derived constants that should not be over written from outside. Option B, runtime variables. Options C port directions. Option D replacing all parameters. Select the best answer.
