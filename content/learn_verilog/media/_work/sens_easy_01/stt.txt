Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog Redz CLK, always at asterisk CLK equals N, RST underscore N, CLK, a sensitivity list tells the simulator. Option A. Which signal events rerun the always block? Option B. Only port directions. Option C. The PLL period. option D which files to compile. Select the best answer.
