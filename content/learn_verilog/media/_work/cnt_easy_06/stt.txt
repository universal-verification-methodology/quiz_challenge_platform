Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash model 11, terminal count 11 async reset clears count. Option A. When reset asserts independent of clock edge per template. Option B. Only on dollar finish. Option C. Only with generate. Option D. Only with C, select the best answer.
