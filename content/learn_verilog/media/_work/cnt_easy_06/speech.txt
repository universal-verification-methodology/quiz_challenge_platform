Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Async reset clears count. Option A. When reset asserts, independent of clock edge (per template). Option B. Only on $finish. Option C. Only with generate. Option D. Only with Z. Select the best answer.
