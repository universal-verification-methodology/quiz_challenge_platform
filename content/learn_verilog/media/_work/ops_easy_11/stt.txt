Look at the browser lab figure on the slide. Question. Given this RTL snippet, Barolog always add, Posegclk, a equals b, forward slash forward slash review context, y, phi, clock, slot 11 variant 1 for multibit vectors, and is underscore underscore underscore underscore underscore while and and is underscore underscore underscore underscore underscore underscore underscore. Option A, bitwise, per bit slash logical, one bit result. Option B logical slash bitwise. Option C identical always. Option D only for TB. Select the best answer.
