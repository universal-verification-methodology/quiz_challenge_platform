Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log-reg, 5 o'clock, count, always at, pos-edge-CLK, if n, count equals count plus 1, forward slash forward slash review context, count, 3 o'clock, slot 18 variant 1 hierarchical counters, bytes, option A ripple enables between stages. Option B must be 1 flat, always only ever. Option C Bancie. Option D, require try. Select the best answer.
