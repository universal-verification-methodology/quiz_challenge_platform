Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Unconnected output ports. Option A. Are usually fine (parent simply ignores them). Option B. Always float the parent's inputs. Option C. Delete the driving logic. Option D. Force Z on all inputs. Select the best answer.
