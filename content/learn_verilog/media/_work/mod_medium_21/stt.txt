Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instant U23, ports wired with .rst underscore n, rst underscore n, forward slash forward slash review context, y, 6 o'clock, slot 21 variant 1.clk, clock, connects. Option A, formal port CLK to actual net clock. Option B, parameter CLK to 1. Option C. Two modules named CLK and clock. Option D. Only a wire declaration. Select the best answer.
