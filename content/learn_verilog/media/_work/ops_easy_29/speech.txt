Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. == vs ===. Option A. === also matches X/Z bitwise; == has X-pessimism rules. Option B. They are identical. Option C. == is only for reals. Option D. === is arithmetic. Select the best answer.
