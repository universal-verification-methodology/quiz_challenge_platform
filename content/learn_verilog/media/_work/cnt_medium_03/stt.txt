Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bear a log forward slash forward slash mod 5, terminal count 20 using count equals count plus 1 with blocking and clocked block as poor flop style.
