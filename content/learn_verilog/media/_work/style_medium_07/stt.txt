Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at Posecclk or Nejidrst underscore n. If RST underscore n, data equals zero, L-data equals count, assertions can encode style slash intent beyond plant names.
