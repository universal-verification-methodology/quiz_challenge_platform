Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Berylog always at POSEDGE CLK or NeJEDGE RST under score N, if RST under score N, D equals zero, LST equals CLK, putting data inputs in a clocked sensitivity list makes a combo block.
