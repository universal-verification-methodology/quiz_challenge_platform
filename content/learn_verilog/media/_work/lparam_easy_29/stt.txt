Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log reg Q, always at asterisk Q equals D. CLK Q forward slash forward slash review context A FITA clock slot 29 variant one teaching reason for local perm. Option A prevent colors from breaking invariance. Option B faster typing. Option C ban math. Option D, Replace Modules. Select the best answer.
