Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. Instance array with named ports. Option A. Each instance gets the same named mapping pattern. Option B. Bans named. Option C. Requires defparam. Option D. Requires wait. Select the best answer.
