Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always at, pos edge CLK, RST underscore N equals data, Pisa with load. Option A captures parallel when load, then shifts out. Option B. Only shifts forever. Option C needs blocking for flops. Option D bans clocks. Select the best answer.
