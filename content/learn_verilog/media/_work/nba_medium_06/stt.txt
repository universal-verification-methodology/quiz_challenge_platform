Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare-log-reg RST-under-score-n, always-at-aster-esque, RST-under-score-n equals data and count, non-blocking with interest-assignment delay Q equals number 1D. Option A simulation delay scheduling, not synth RTL delay. Option B creates PLL. Option C required FPGA style. Option D ANSI only. Select the best answer.
