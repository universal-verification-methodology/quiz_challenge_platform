Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Dara log always at Pose edge CLK or Nej edge RST underscore N. If RST underscore N, data equals zero, L state of equals count, both Pose edge and Nej edge of the same clock in one always as unusual slash wrong for a simple flop.
