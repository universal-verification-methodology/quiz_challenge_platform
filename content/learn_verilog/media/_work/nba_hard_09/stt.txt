Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog always at, Posegclk, some equals din, lint, blocking used in sequential. Option A, style warning to prefer NBA and clocked blocks. Option B means syntax error always. Option C means delete reset. Option D means ad x, select the best answer.
