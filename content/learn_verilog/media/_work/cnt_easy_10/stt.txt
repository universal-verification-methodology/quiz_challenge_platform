Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log always at, pos edge CLK, if, load, count equals 10, else if, CE, count equals count plus 1, counters are usually coded with non-blocking updates.
