Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog Property P20, At, Pose edge CLK, D-Hashtag hashtag 1CLK, and Property Forward slash Forward slash Review context, RST underscore N, 31 colon zero, slot 16 variant 1 sensitivity to Pose edge CLK, zero, on a vector clock. Option A triggers on that bits rising edge. Option B, illegal always. Option C means whole bus edge. Option D sets width. Select the best answer.
