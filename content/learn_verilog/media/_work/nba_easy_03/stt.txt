Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log ridge RST underscore N, always at asterisk, RST underscore N equals data and count, A equals B, B equals A, with A equals 1, B equals 0 before edge. Option A, A becomes 0, and B becomes 1, a real swap. Option B. Both unchanged. Option C. Both 0. Option D. Both one. Select the best answer.
