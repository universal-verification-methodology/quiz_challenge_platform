Look at the browser lab figure on the slide. Question. Given this RTL snippet, their log always at, PosegCLK, begin RST underscore N equals data, data equals RST underscore N, and forward slash forward slash review context, A, for a clock, slot 19 variant 1-Lint, blocking used in sequential. Option A, style warning to prefer NBA and clock blocks. Option B means syntax error always. Option C means delete reset. Option D means add X, select the best answer.
