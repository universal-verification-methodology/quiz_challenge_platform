Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Barolog unique case, n, forward slash asterisk, full case required asterisk forward slash, you cannot use a non-constant red value as a generative condition.
