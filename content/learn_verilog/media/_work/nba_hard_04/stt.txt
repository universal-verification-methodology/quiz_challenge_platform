Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log-reg-RST underscore N, always at, asterisk, RST underscore N equals data and count, dual always writing same-reg one with equals one with equals. Option A, broken multi-driver slash race nightmare. Option B, standard dual port. Option C, required CDC. Option D. Only SB interfaces. Select the best answer.
