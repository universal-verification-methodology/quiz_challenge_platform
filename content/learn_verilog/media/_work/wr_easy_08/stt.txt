Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log wire, 5 o'clock, some assigned an equal some carrot count, driving a wire from an always block without a continuous assign. Option A. Is illegal, need a variable. Option B. Is preferred. Option C creates a PLL. Option D forces Z. Select the best answer.
