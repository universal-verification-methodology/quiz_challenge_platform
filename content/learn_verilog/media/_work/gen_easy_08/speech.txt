Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. assign y = concat WIDTH concat 1 bit binary 0 ;. Option A. Zero vector WIDTH bits. Option B. WIDTH modules. Option C. A flop chain. Option D. Illegal concat. Select the best answer.
