Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog unique case, RST underscore N, forward slash asterisk full case required asterisk forward slash forward slash forward slash review context, A, for a clock, slot 19 variant one assigning to an input port from inside the module. Option A. Is a legal slash bad, inputs are driven externally. Option B. is Reset Style. Option C creates a flop. Option D sets width. Select the best answer.
