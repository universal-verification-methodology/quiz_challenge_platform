Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always underscore FF-AT, posedgeCLK, BeginF, C, D equals D plus 1 foot B1, N style versus functionality conflict. Option A, correctness wins, then align style. Option B, style wins over bugs. Option C, ignore both. Option D, delete tests. Select the best answer.
