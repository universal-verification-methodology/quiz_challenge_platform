Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instance U31, ports wired with .a, a forward slash forward slash review context, data three o'clock, slot 27 variant one cross module force on a port in RTL. Option A. Is test bench oriented and not synthesizable design style? Option B. is the standard reset method. Option C replaces always underscore FF. Option D is required for named ports. Select the best answer.
