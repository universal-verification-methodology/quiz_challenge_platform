Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log-reg, to a clock, count, always at, pos-edge CLK, if n, count equals count plus 1, almost full-style counts. Option A, compare Q to a threshold. Option B, require multi-driver. Option C, ban and BA. Option D, use only dollar display. Select the best answer.
