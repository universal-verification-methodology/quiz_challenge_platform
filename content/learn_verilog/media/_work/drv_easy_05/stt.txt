Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log-redge count, always at asterisk count equal cell out count preferred bus-mucks style in RTL. Option A single assigned slash always mucks not to enable drives fighting. Option B to assigns both on. Option C force in RTL. Option D only initial. Select the best answer.
