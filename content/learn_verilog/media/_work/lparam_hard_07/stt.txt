Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Barilog unique case, cell, forward slash asterisk, full case required slash instance, override of width updates local perm depth equals width 2 in that instance.
