Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at, pos edge CLK, beginning equals B, B equals A, and forward slash forward slash review context. A, 5 o'clock, slot 29 variant 1 blocking chain and clocked block creating combo path into next NBA. Option A. Can infer unexpected logic, avoids spaghetti. Option B preferred. Option C required for lint. Option D. Only legal with try. Select the best answer.
