Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always at, pos edge CLK, begin CLK equals N, N equals CLK, and read before write with NBA in one edge. Option A, RHS C's old value. Option B, RHS always sees new. Option C needs wire. Option D needs generate. Select the best answer.
