Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog always at, PosegCLK, Y equals Q, forward slash forward slash review context, count for a clock, slot 28 variant 1 pipeline shift with NBA, Q equals Q, 6 o'clock, SI. Option A registers the new vector next edge. Option B, combo shift only. Option C illegal. Option D needs two clocks always. Select the best answer.
