Look at the browser lab figure on the slide. True or false? Given this RTL snippet, BareLog unique case, N, forward slash asterisk, full case required asterisk, forward slash positional mapping uses the ANSI slash non-ANSI header order of port names.
