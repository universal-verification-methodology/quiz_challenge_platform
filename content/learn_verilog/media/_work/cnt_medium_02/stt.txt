Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log reg, 5 o'clock, count, always at, posetzclk, if n, count equals count plus 1, mod n for non-power of 2n. Option A, compare to n1 and recycle to 0. Option B, automatic with any width. Option C, impossible. Option D needs dual clock. Select the best answer.
