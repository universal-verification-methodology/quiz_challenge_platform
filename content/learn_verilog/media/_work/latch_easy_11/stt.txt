Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at, pos edge CLK, a equals B, forward slash forward slash review context, y, phyto clock, slot 11 variant 1L latch and combinational RTL usually means. Option A. The output can hold its previous value when inputs change. Option B. Every input combo drives every cycle. Option C. No sensitivity list. Option D assign a legal. Select the best answer.
