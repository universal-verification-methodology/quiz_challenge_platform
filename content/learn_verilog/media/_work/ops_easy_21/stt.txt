Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log red cell, always at asterisk, cell equals out. Some, cell, forward slash forward slash review context, Y, six o'clock, slot 21 variant one for multibit vectors, and is underscore underscore underscore underscore underscore underscore wall and and is underscore underscore underscore underscore underscore underscore underscore. Option A Bitwise Perbit slash logical, one bit result. Option B logical slash bitwise. Option C identical always. Option D only for TB. Select the best answer.
