Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log wire, 3 o'clock, D assigned CLK equals D, instantiating mux 2u0, creates. Option A. One instance of that module. Option B. A parameter override only. Option C. A continuous assign. Option D. A generate loop. Select the best answer.
