Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylug unique case, RST underscore N, forward slash asterisk full case required asterisk forward slash forward slash forward slash review context, A, for a clock, slot 19 variant one computation dollar clog two, depth plus one, as local perm. Option A, common for pointer widths with extra state. Option B, illegal math. Option C creates latch. Option D meets NBA. Select the best answer.
