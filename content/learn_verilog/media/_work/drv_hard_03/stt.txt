Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Bare-Log unique case, N, forward slash asterisk, full case required asterisk forward slash a clock, Net should have a single intentional driver, plus buffers as designed.
