Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Bare-Lug unique case, N, forward slash asterisk, full case required slash a red used only in combo, always at, can still synthesize to gates without a flop.
