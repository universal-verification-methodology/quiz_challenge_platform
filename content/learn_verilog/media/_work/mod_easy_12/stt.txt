Look at the browser lab figure on the slide. Question. Given this RTL snippet, BARA-Log module M17, input CLK, input B, output Y, and module forward slash forward slash review context, Q, 6 o'clock, slot 12 variant 1 and input port on a module. Option A. Is driven from outside into the module? Option B must always drive the parent. Option C. is the same as a parameter. Option D cannot appear on a gate like in two. Select the best answer.
