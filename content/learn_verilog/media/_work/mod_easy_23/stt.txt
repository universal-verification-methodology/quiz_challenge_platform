Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire, two o'clock, some assigned an equal sum, forward slash forward slash review context, d, 11 o'clock, slot 23 variant one named port connection.y, y means. Option A, connect formal port y to actual net y option B, delete port y from the module. Option C, declare a new module named y option D, set a parameter named Y only. Select the best answer.
