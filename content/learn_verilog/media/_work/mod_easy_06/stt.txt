Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash instant U11, ports wired with .sell, cell, ports appear in the module. Option A header, and optionally body for older style. Option B. Only inside always blocks. Option C. Only in dollar display calls. Option D. Never, modules have no IO. Select the best answer.
