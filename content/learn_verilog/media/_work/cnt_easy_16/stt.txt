Look at the browser lab figure on the slide. Question. Given this RTL snippet, Berylog always at Posegclk, if load, count equals 10, else if, c, count equals count plus 1, forward slash forward slash review context, RST underscore n, 31 colon zero, slot 16 variant 1A sync reset clears count. Option A. When reset asserts, independent of clock edge, per template. Option B. Only on dollar finish. Option C. Only with generate. Option D. Only with C. Select the best answer.
