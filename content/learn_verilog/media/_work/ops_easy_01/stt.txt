Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log Red CLK, always at asterisk CLK equals n. RST underscore n, CLK, for multibit vectors, and is underscore underscore underscore underscore underscore underscore underscore underscore underscore underscore underscore underscore underscore underscore. Option A, bitwise, permit slash logical, one-bit result. Option B logical slash bitwise. Option C identical always. Option D only for TB. Select the best answer.
