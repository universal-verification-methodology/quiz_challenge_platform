Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log-redge count, always at asterisk, count equal cell, out, count to avoid latches and combo if slash else. Option A, cover all paths slash default assignments. Option B, remove at asterisk. Option C, use only hashtag delay. Option D, ban turnery. Select the best answer.
