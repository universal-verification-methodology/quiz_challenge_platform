Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log always at, pos edge CLK, if, load, count equals den, else if, c, count equals count plus 1, clear and enable both true need to find priority in the the entry.
