Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Dara-Log always at POSEDGE CLK, Data equals count, Flip Flop RTL and always at POSEDGE CLK, should normally use equals for register updates.
