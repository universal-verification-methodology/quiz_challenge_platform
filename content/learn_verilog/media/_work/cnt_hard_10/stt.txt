Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log forward slash forward slash mod 14, terminal count 20 teaching bug, if, c, q equals q plus 1, else q equals q plus 1. Option A enabled is nothing, always increments. Option B holds correctly. Option C clears. Option D tries states. Select the best answer.
