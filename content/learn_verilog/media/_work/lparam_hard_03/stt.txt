Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Barilog unique case, N, forward slash asterisk, full case required asterisk, forward slash hierarchical depth perm to a local perm should be rejected.
