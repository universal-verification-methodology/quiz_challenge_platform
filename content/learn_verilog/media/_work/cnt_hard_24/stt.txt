Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare a log-reg, 2 o'clock, count, always at, pos-edge CLK, if n, count equals count plus 1, forward slash forward slash review context, CLK, 15 colon 0, slot 24 variant 1 CDC, gray pointer into other clock. Option A, synchronize gray, then convert. Option B, sample binary multibit directly safely always. Option C Use Force. Option D Use Only blocking. Select the best answer.
