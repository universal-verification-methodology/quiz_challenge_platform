Look at the browser lab figure on the slide. True or false? Given this RTL snippet, bare log always at, POSEDGE CLK, if, load, count equals 10, else if, C, count equals count plus 1, sync reset clears on clock edge while reset is asserted.
