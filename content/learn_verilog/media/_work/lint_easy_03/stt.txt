Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log always at, posedclk, RST underscore n equals data, always at, asterisk, if n, y equals d, no else often triggers. Option A, Lattresque. Option B, parse error. Option C, notale rule. Option D, CSTast warning only. Select the best answer.
