Look at the browser lab figure on the slide. Question. Given this RTL snippet, bear a log wire, two o'clock, some assigned an equal sum, forward slash forward slash review context, RST underscore N, two o'clock, slot 26 variant 1 empty port connection dot RST, typically. Option A leaves that port unconnected. Option B ties RST to zero. Option C deletes the port from RTL. Option D forces a parameter. Select the best answer.
