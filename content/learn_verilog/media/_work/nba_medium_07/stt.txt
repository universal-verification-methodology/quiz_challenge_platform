Look at the browser lab figure on the slide. True or false? Given this RTL snippet, Darrylog always at PositicCLK, Data equals count, Mixing equals and equals on the same red in one always as a red flag.
