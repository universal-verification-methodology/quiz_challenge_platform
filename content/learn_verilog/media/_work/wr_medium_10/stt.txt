Look at the browser lab figure on the slide. Question. Given this RTL snippet, barrel log the sign out equals, some and in, some carrot den, initial procedural assigned to a reg. Option A runs once at time zero, simulation. Option B is synthesizable reset everywhere. Option C drives a wire continuously. Option D sets a parameter. Select the best answer.
