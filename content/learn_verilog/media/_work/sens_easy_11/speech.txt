Look at the browser lab figure on the slide. Question. Given the RTL snippet on the slide. A sensitivity list tells the simulator. Option A. Which signal events re-run the always block. Option B. Only port directions. Option C. The PLL period. Option D. Which files to compile. Select the best answer.
