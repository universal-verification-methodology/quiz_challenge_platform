Look at the browser lab figure on the slide. Question. Given this RTL snippet, bare log always at posedclk a equals b forward slash forward slash review context y five o'clock slot 11 variant 1a sensitivity list tells the simulator option a which signal events rerun the always block option b only port directions option c The PLL period. Option D. Which files to compile. Select the best answer.
