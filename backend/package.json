{
  "name": "quiz-challenge-results",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "4.86.0"
  },
  "engines": {
    "node": ">=20.3.0"
  }
}
